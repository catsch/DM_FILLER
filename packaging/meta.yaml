{% set version = "2.6" %}
{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-dmfiller
  version: {{ version|replace("-", "_") }}

source:
  url: https://github.com/fair-ease/CANYON-B/archive/refs/tags/v.{{ version }}.tar.gz #VR   ???
  sha256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 #VR   ???

build:
  merge_build_host: true  # [win]
  number: 0
  noarch: generic
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ posix }}zip               # [win]
    - cross-r-base {{ r_base }}    # [build_platform != target_platform]  
    - r-devtools
    - r-roxygen2
  host:
    - r-base
    - r-mgcv
    - r-seacarb
    - r-tibble
  run:
    - r-base
    - r-mgcv
    - r-seacarb
    - r-tibble

test:
  commands:
    - $R -e "library('canyonb')"           # [not win] #dm_filler
    - "\"%R%\" -e \"library('canyonb')\""  # [win] #dm_filler

about:
  home: https://github.com/HCBScienceProducts/CANYON-B/tree/v0.9 #VR   https://github.com/catsch/DM_FILLER
  license: GPL-3.0-or-later 
  license_file: LICENSE
  license_family: GPL
  summary: CANYON-B method published in Bittig et al. (2018). An alternative to static climatologies Robust estimation of open ocean CO2 variables and nutrient concentrations from T, S and O2 data using Bayesian neural networks. Front. Mar. Sci. subm. http://dx.doi.org/10.3389/fmars.2018.xxxxx.
#VR    DM_FILLER reports BGC delayed mode information into argo files according to ARGO recommandations
extra:
  recipe-maintainers:
    - conda-forge/r
    - marie59 #VR il me faut un compte conda ou git?
